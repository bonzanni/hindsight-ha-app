*.pyc
__pycache__/
.DS_Store
.worktrees/
.devdata/
# Internal engineering docs are a SEPARATE PRIVATE inner repo (docs/.git ->
# bonzanni/ha-hindsight-app-docs). Never tracked in this public add-on repo.
# A .githooks/pre-commit guard (installed by `make setup`) refuses staging docs/.
docs/
node_modules/
tests/test-results/
test-results/
playwright-report/
# stray HOME caches from running npx/playwright in a container with HOME=/work
.npm/
.local/

# local prod-console config (host/slug details)
.claude/
